# CLI Examples

```bash
python vorpy example.pdb
python vorpy example.pdb -s nt pow
python vorpy example.pdb -s nt prm
python vorpy example.pdb -s sr 0.05
python vorpy example.pdb -g a 0-100
python vorpy example.pdb -e small
python vorpy example.pdb -e small and shell
```

**TODO:** Add validated protein, DNA/RNA, ligand, residue, chain, interface, and curvature workflows.
