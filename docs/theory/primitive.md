# Primitive Voronoi

The primitive diagram uses:

```text
d_prm(x, i) = ||x - c_i||
```

Generator radii do not affect boundary placement, making this a useful radius-independent reference.

VorPy name: `prm`.
