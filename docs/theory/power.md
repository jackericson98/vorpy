# Power / Laguerre Diagrams

The power diagram uses:

```text
d_pow(x, i) = ||x - c_i||^2 - r_i^2
```

It incorporates generator size while retaining planar boundaries. Power and additively weighted partitions are generally different when radii are unequal.

VorPy name: `pow`.
